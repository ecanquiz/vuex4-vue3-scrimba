//import { getters } from "../../store";

describe('getters', () => {
  it('filteredProducts', () => {
    // mock state
    
    // mock getter 

    // get the result from the getter

    // assert the result
    
  })
})
