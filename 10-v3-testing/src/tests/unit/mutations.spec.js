//import { mutations } from "../../store";

//const { increment } = mutations

describe('mutations', () => {
  it('INCREMENT', () => {
    // mock state

    // apply mutation

    // assert result

  })
})
